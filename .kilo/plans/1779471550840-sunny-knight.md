# Plan: Add Username/Password Fields to Employee Forms

## Summary
Add username and password input fields to the Add Employee form, and add username/password fields (pre-filled from database) to the Edit Employee form. The password should be optional in edit mode.

## Analysis of Current State

### add-employee.html
- **HTML:** Already has a "Login Credentials" section (lines 281-317) with username, password, confirmPassword, and userRole fields
- **JavaScript:** The form submit handler at line 360-431 extracts employee data but does NOT include username/password in the payload
- **Need:** Update JS to include credentials and create user account after employee creation

### edit-employee.html
- **HTML:** NO username/password fields exist - needs to be added
- **JavaScript:** Has `loadEmployeeData()` and form submit handler
- **Need:** Add HTML section, fetch linked user, populate/update user credentials

### edit-employeeSadmin.html
- **Observation:** This file is actually an EMPLOYEE LISTING page (same as employeesSadmin.html)
- **Issue:** The `editEmployee()` function at line 565 navigates to `edit-employeeSadmin.html?id=X`, but this file shows a table/list view, not an edit form
- **Recommendation:** Either convert this to an edit form OR use `edit-employee.html` for all edits

## Implementation Plan

### 1. Fix add-employee.html (Minor Fix)
**File:** `add-employee.html`

The HTML already has credentials fields. Need to update the JavaScript submit handler:
- Capture `username`, `password`, and `role` values from the form
- After employee creation, call `/api/users.php` to create user account with `linked_employee: newEmployeeId`

### 2. Update edit-employee.html (New Feature)
**File:** `edit-employee.html`

Add after Emergency Contact section (around line 343):
```html
<!-- Login Credentials -->
<div class="pt-4 border-t border-gray-100">
    <h2 class="text-lg font-semibold text-gray-800 mb-4 flex items-center gap-2">
        <i class="fa-solid fa-key text-[#b0303b]"></i> Login Credentials
    </h2>
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">Username <span class="text-red-500">*</span></label>
            <input type="text" id="username" required 
                class="w-full px-4 py-2 border border-gray-200 rounded-lg focus:ring-2 focus:ring-[#b0303b]">
        </div>
        <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">Password</label>
            <input type="password" id="password" 
                class="w-full px-4 py-2 border border-gray-200 rounded-lg focus:ring-2 focus:ring-[#b0303b]"
                placeholder="Leave blank to keep current">
        </div>
        <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">Confirm Password</label>
            <input type="password" id="confirmPassword" 
                class="w-full px-4 py-2 border border-gray-200 rounded-lg focus:ring-2 focus:ring-[#b0303b]">
        </div>
        <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">User Role</label>
            <select id="userRole" class="w-full px-4 py-2 border border-gray-200 rounded-lg focus:ring-2 focus:ring-[#b0303b]">
                <option value="teacher">Teacher / Faculty</option>
                <option value="admin-staff">Admin Staff</option>
                <option value="guard">Security Guard</option>
                <option value="sa">Student Assistant</option>
            </select>
        </div>
    </div>
</div>
```

Update `loadEmployeeData()`:
- Fetch linked user: `GET /api/users.php?linked_employee={employeeId}`
- Populate username, userRole fields
- Store the userId for later update

Update form submit handler:
- Collect credential values
- Validate password if provided (min 6 chars, match confirm)
- Send PUT to `/api/users.php` with updated credentials

### 3. Resolve edit-employeeSadmin.html confusion
**Question for user:** Should `edit-employeeSadmin.html` be:
- A dedicated edit page (needs full form like edit-employee.html)
- Or should we use `edit-employee.html` for all edits?

## Files to Modify
1. `add-employee.html` - Fix JS to include credentials in submission
2. `edit-employee.html` - Add credentials section and JS logic
3. `js/edit-employeeSadmin.js` - Update to handle credentials (OR fix the HTML first)

## API Endpoints Available
- `POST /api/users.php` - Create user (requires username, password, role)
- `PUT /api/users.php` - Update user (id required, password optional)
- `GET /api/users.php?linked_employee={id}` - Find user linked to employee

## Notes
- Password in edit mode is optional - leave blank to keep existing password
- If no linked user exists, create one
- If linked user exists, update it